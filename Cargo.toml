[package]
name = "benkyou"
version = "0.3.0"
edition = "2021"
description = "Learn a new domain quickly: prerequisite graph, assessment, cards and graded exercises."
license = "MIT"
repository = "https://github.com/brickfrog/benkyou-yaro"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"

[profile.release]
strip = true
