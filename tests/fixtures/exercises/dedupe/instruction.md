# Order-preserving dedupe

Implement `dedupe(xs)` in `solution.py`.

It returns a new list holding each element of `xs` exactly once, in the order each
first appeared. The input is not modified. Elements are hashable.

```python
dedupe([3, 1, 3, 2, 1]) == [3, 1, 2]
dedupe([]) == []
```
